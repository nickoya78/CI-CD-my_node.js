{
  "name": "my-nodejs-app",
  "version": "1.0.0",
  "description": "A simple Node.js application",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha tests/**/*.js",
    "build": "mkdir -p build && cp src/index.js build/"
  },
  "devDependencies": {
    "mocha": "^9.2.2"
  }
}
